# Iorio Reloaded — Progress Tracker

Last updated: 2026-08-28. Streamlined 2026-08-27 — resolved/shipped narratives condensed to 1-3 lines each; full debugging detail for anything condensed here lives in git history for the date mentioned. Open/unresolved items kept in full.

**Positions screen UI pass, shipped 2026-08-28** (`iorio-reloaded-app`): six items from a screenshot review. (1) Numeric font (Roboto Mono/tabular-nums) is now platform-wide, applied via the two conventions every numeric value already follows — `DataTable`'s `text-end` cells and the `badge-change-*` price badges — rather than touching every call site. (2) Positions' Opened/Expiry columns now show a relative label only ("today"/"yesterday"/"2 days ago"/"in 7 days") with the real date moved to a hover tooltip; new `formatDaysAgo`/`daysAgo` helpers in `formatters.ts`, and `formatDaysToExpiry`'s "in 1 day" case reworded to "tomorrow". (3) SMH showing open past its Aug 27 expiry investigated directly against live IBKR (`fetchIbkrHeldPositions`) — confirmed IBKR itself still reports the short put as held; not an app bug, just IBKR's own settlement lag, self-clears once IBKR processes it. (4) New MV (Market Value) column, `= EXP $ + P&L $` (approved formula). (5) **Real bug found and fixed**: the column-visibility gear icon on every `DataTable` app-wide was a dead button — `ColumnVisibilityPopover.tsx` used Bootstrap's `data-bs-toggle="dropdown"`, but the app never loads Bootstrap's JS bundle (only `@tabler/core`'s CSS), so the dropdown never had any JS to open it. Rebuilt as a manually-driven dropdown (open state + click-outside-to-close), fixing the gear on every table platform-wide via this one shared component. (6) Dark-mode `--tblr-border-color` was `~#2e3d51` against a `#1f2937` body — nearly invisible; lightened to `#45566e` globally in `theme.css`, fixing card/table/tab border visibility across the whole app, not just Positions. All six verified live via Playwright in both themes; typecheck + build clean. Commit `10be792`.

**Background jobs / toast notifications, built and Playwright-verified 2026-08-27** (`iorio-reloaded-app`): a shared `BackgroundJobsContext` (mounted in `AppLayout`, so it survives all client-side route changes) turns the trade-alert scan and order confirm/cancel updates into persistent toasts instead of page-scoped state — closing/navigating away from the page that started them no longer kills progress visibility. Toasts stay open until manually dismissed (X) and automatically reopen the instant there's real news (a status/message change). Trade Alerts page also now live-renders each ticker/roll result into its table as the scan finds it, rather than waiting for the whole scan to finish.

**Order polling replaced with a push SSE notification stream, 2026-08-27** (both apps): the order-status side of the toast system above originally client-polled `GET /orders/:id` every 2s, which only ever surfaced orders this browser itself started via `startOrderJob` — an order confirmed via Genosuke chat, or a position IBKR closed on its own (an option expiring/being assigned), never showed up anywhere in the UI. Replaced with one persistent `GET /notifications/stream` SSE connection per browser tab (`notifications.ts`/`notificationBroadcaster.ts`), fed by a single Postgres `LISTEN` on a new `app_notifications_channel` that both the web dyno (order confirm/cancel) and the worker (IBKR-reported fills/errors/expiries) `pg_notify` into — so the source of the change doesn't matter, every connected tab hears about it. `BackgroundJobsContext` now re-fetches and upserts the order job on each `order_status` event instead of polling, and a new `position-closed` toast kind renders `notifyPositionExpired`'s message text directly (same text sent to Telegram, single source of truth). Type-checked/tested. Marcelo confirmed a live order himself against prod 2026-08-27 to verify this end-to-end (a local-dev-DB attempt earlier that session didn't count — the VPS worker only watches prod's `order_requests`, not local dev's).

**Order-id collision bug found, fixed, deployed, and prod history repaired — 2026-08-27**: the new toast system surfaced two separate "SMCI — Open Order" toasts for what looked like one order — turned out to be two real `order_requests` rows sharing `ibkr_order_id = 5`, one a two-day-old row stuck non-terminal from a prior Gateway session, one today's real order. IBKR's `orderId` is only unique within one Gateway connection session and gets reused after every restart (the worker also restarted 5+ times today alone during testing, so ids 1-6 were each reused across 2-4 unrelated orders); `ibkrGatewayWorker.ts`'s fill/status listener matched purely on that number with no session-scoping, so a single real fill could flip every historical row sharing that id. Real position/P&L data was never at risk — `trades`/`positions` are keyed by IBKR's unique `ibkr_exec_id`/live holdings, not `order_requests.status` — but the audit-trail column itself was actively corrupting in prod through the whole testing session.

Fix, deployed to the VPS worker same day: `order_requests.ibkr_perm_id` (already in the schema since 2026-08-24, never wired up) is now captured on an order's first status callback and required to match on subsequent updates — IBKR's `permId` is globally unique forever, unlike `orderId`. A row already in a final status (`filled`/`cancelled`/`rejected`/`error`) is now excluded from ever being matched again, closing the gap where a terminal row's `ibkr_order_id` could still be hit by a future reuse. Added `reconcileStaleOrderRequests()` (`ibkrGatewayFetchOpenOrders.ts`), run at worker startup and on every reconnect: any local `submitted`/`partially_filled`/`cancel_requested` row IBKR's `reqAllOpenOrders()` no longer reports gets flagged `error` and its `ibkr_order_id` cleared before a reused id can hit it.

Prod history repair (one-off `tmp/` script, Marcelo ran it against prod `DATABASE_URL` — the harness's auto-mode classifier blocks Claude from running writes against prod directly): every corrupted row was reconciled against its actual matching trade by `(ibkr_order_id, same calendar day, same symbol)` instead of trusting its corrupted status. 11 rows had a real fill hidden behind a wrong `submitted`/`cancelled` status and were corrected to `filled` (including the SMCI order from the bug report, and one AAOI open that had been wrongly marked `cancelled` despite a real fill); 8 had no matching trade at all — including one, an AAOI CSP, that had falsely shown `filled` with zero supporting execution — and were marked `error`/unconfirmable with their source trade alert reverted to `pending`. 6 rows from today were left untouched as still genuinely in flight.

## Vision
Multi-strategy options trading system. Business user checks in daily, reviews suggested opportunities and open positions, approves/rejects/modifies trades. Trades execute (semi-automated) via IBKR once confirmed. Node.js + React + Postgres, hosted on Heroku.

## Status
Both apps auto-deploy from GitHub on push to `main`, hosted on Heroku (`app.ioriore.com` / `api.ioriore.com`). **`iorio-reloaded-api` (backend) is on a Basic web dyno** (switched back 2026-08-25 after a one-day Eco experiment — Eco's 30-min idle-sleep was suspected of correlating with Scheduler one-off dynos getting SIGTERM'd mid-run; moderate confidence, not worth the ~$14/mo saved since this app runs all 6 Scheduler jobs). **`iorio-reloaded-app` (frontend) is on Eco** (runs no scheduler jobs, no exposure to that risk). Heroku Scheduler's own per-job dyno size is still Eco for all 6 jobs (no CLI/API access to change it — set manually in the dashboard if needed). IBKR paper Gateway live on a Hetzner VPS via a restricted SSH tunnel; `ibkrGatewayWorker.ts` also runs there as `iorio-worker.service` (systemd, `Restart=always`) — see "Order execution platform" below. **All 7 screens + Price Performance (8th) built.** 6 scheduled jobs installed on Heroku Scheduler (see that section — one is currently broken, see below). Telegram alerting live. Prod migrations current (`iorio-reloaded-api` v31+, `iorio-reloaded-app` v17).

**Major architecture change, started 2026-08-24, mostly complete:** Iorio moved from a manually-entered position ledger to a real order-placing/monitoring platform where IBKR is the source of truth. Full detail in "Order execution platform" below — order placement, fills, closes, cancels, and rolls are all verified against real paper trades; a few edge paths remain untested (see that section's open items).

## Currently open / needs attention
These are the live threads — everything else below this point is either resolved (kept for context) or long-term backlog.

- **Daily price/IV capture job (`job:daily-market-data`, 9:00 PM UTC) — root cause found and a detection/auto-fix shipped 2026-08-27, not yet confirmed against tonight's real run.** Had failed 100% of tickers on every scheduled run for 4+ straight trading days (08-24 through 08-27). The "runs too soon after close" theory (tied to the 9PM UTC slot) was tested and falsified live 2026-08-27: `reqHistoricalData` was found hanging with zero response (no data, no error event, just a 20-25s timeout) at 12:24 UTC — mid-morning, nowhere near market close — for every real shortlist ticker tried, while the connection handshake and every other IBKR call (health check, position reconciliation) stayed healthy throughout. Restarting the paper Gateway container on the VPS cleared it immediately (confirmed against 3 previously-hanging tickers, all returned in under 3.5s afterward); the restart's IBC log showed a settings-file recovery (`ibg.xml` missing, recovered from backup), suggesting the container's historical-data subsystem had gotten into a bad state that a handshake-only check can't see. **Fix shipped**: `ibkr_health_check` (hourly) now also probes `reqHistoricalData` (fixed SPY symbol) after a successful handshake, and triggers the same auto-restart path if that hangs too — closing the exact gap that let this run undetected for 4 days. Next step: confirm tonight's 9PM UTC run succeeds; if it recurs, the underlying trigger for the stuck historical-data subsystem is still unknown (not conclusively pacing, not conclusively settings corruption — only the symptom and the fix-that-works are confirmed). See [[project_ibkr_historicaldata_reqpnl_pacing]].
- **Real-time market data: still not working, picking up in a new session.** Paper account confirmed delayed-only regardless of Juan's live-account subscription activations (error 10089 on `MarketDataType.REALTIME`, both before/after his changes — moderate confidence paper accounts never inherit live entitlements at all). Live account (`U18059120`) is logged into the VPS's `ib-gateway-live` container (port 4001) for the first time, but our own client still can't complete a connection to it — IBKR closes the socket immediately post-handshake with zero error event and zero Gateway log entry. Not a TrustedIPs/tunnel issue (checked both). Leading theory: the live account's "Enable ActiveX and Socket Clients" API-access permission (Client Portal → Account Settings → API) has likely never been turned on, separate from the market-data subscriptions already enabled. A one-time "Login Messages" dialog also has to be manually dismissed via macOS Screen Sharing on every container restart (the container's VNC has a rendering bug, `-ncache_cr`, worked around but not fixed permanently — see `docker exec ... pkill x11vnc; x11vnc -display :1 ... ` workaround in git history 2026-08-27 if needed again). **Next steps in order**: (1) confirm with Juan whether live-account API access + trusted IPs are enabled in Client Portal; (2) dismiss the current Login Messages dialog; (3) retry `tmp/checkRealtimeDataEntitlement.ts` against `IBKR_TRADING_MODE=live`; (4) only once live connects and shows real-time, flip `MarketDataType.DELAYED → REALTIME` across the 10 hardcoded call sites and run the full IBKR API audit (see "HIGHEST PRIORITY" item under Open decisions below).
- **Investigate live/paper IBKR connection conflicts** [HIGH PRIORITY, Marcelo 2026-08-26] — running both Gateway containers side by side, any live-side connection disconnects other live-side connections, and a real-time subscription shared from live only works on paper while live is disconnected. Not yet investigated. Live gateway was stopped manually 2026-08-26 while diagnosing (not a permanent state).
- **Trade-alert job/notification redesign (roll batch first, then per-ticker streaming) — built and committed 2026-08-27, not yet live-tested.** Dry-run harness confirmed correct event ordering with real IBKR/Postgres calls (Telegram stubbed), but markets were closed so actual message content/formatting for both the roll batch and per-ticker messages hasn't been seen live. Confirm on the next scheduled run during market hours. Also per Marcelo's decision, the manual "Run Now" button no longer sends any Telegram notification — only the scheduled job does.
- **Roll modal's new live-quote streaming — code-reviewed and typechecked, not live-tested.** No roll alert was pending during the 2026-08-27 session to click through for real. Worth a live check next time one exists.
- **`iorio-reloaded-app` frontend fix pending push** — Positions list was showing a rolled-away leg (closed, `exit_at` set) alongside the still-open leg in the compact structure summary (e.g. AAOI's old $114C/100sh next to the live $107P) because `structureSummary` in `PositionsPage.tsx` didn't filter by `exitAt`. Fixed 2026-08-27 (commit `34ad1db`, filters to open legs only when `position.status === "open"`), typechecked, but not yet pushed to `origin/main` so Heroku hasn't rebuilt/deployed it — still visible live as of this writing.
- **Order Review compliance gating — confirmed live 2026-08-27.** Opened an AAOI $125C covered-call opening order during market hours: live quote streamed correctly (bid/ask, IV, delta, theta, vega), no compliance-block red footnote, "Confirm & Submit to IBKR" enabled — matches the expected compliant render path.
- **Yield metric follow-up, not yet written up**: Marcelo asked for a note listing which of Juan's documented strategy parameters (IV Rank, a trend/MA filter for CSPs, bid-ask spread as a fraction of premium) the current `(premium/capitalAtRisk)×(365/dte)` yield/ranking does NOT account for. No schema/ranking change planned until this is written and discussed.
- **Open, not yet built: re-write a covered call after non-assignment (naked stock left over).** When a covered call's short call expires worthless/isn't assigned, nothing today flags the resulting stock-only leg. Wanted: (1) detection of any open `covered_call` position down to stock-only, surfaced on both Dashboard and Positions; (2) a "Sell Call" button on the flagged Positions row that opens the existing Ticker Detail modal pre-scoped to that symbol; (3) fix the New Position covered-call share-quantity calc to account for shares already held instead of assuming a fresh lot from zero. No design pass done yet on the detection query or the Dashboard notification's shape.
- **Two untested order-platform edge paths, carried over from 2026-08-24**: a lone short call placed directly in TWS surfacing as `unstructured`, and closing a position directly in TWS with the worker detecting it. Both need Marcelo/Juan's TWS access directly, not checkable from a session alone.
- Known, accepted gaps: no partial-quantity/partial-leg closes across mixed legs (only whole-position or option-leg-driven downsizing exists); Screener's "Avg Vol" column is empty for every ticker (not investigated); Position Detail chart intermittently hits `reqHistoricalData` pacing timeouts (expected IBKR behavior, not a code bug).
- **User-attribution audit — audited, proposal approved, and shipped 2026-08-28.** Found: `order_requests.requested_by_user_id`/`trade_alerts.reviewed_by_user_id` were already correctly captured on every write (including Genosuke's, which authenticates as its own real `users` row with `display_name = "Genosuke"`), but both were silently dropped before reaching the API response, so nothing ever reached the UI. Fixed: `serializeOrderRequest()`/`tradeAlertSelect` now join and return the requester/reviewer's display name; Trade Blotter, Trade Alerts (non-pending view), and Risk & Limits now show it. Also added and wired up two columns that had zero attribution before: `order_requests.cancelled_by_user_id` (who clicked Cancel, captured independently of who originally requested the order — verified with a real Juan-requests/Marce-cancels case) and `strategy_settings.updated_by_user_id`. Migration `20260828000002`. Along the way, found and fixed a dead route: Genosuke's `reject_trade_alert` tool called `PATCH /trade-alerts/:id`, which never existed (404 on every call, likely orphaned when per-alert Dismiss/Reject was removed from the UI 2026-08-27) — now implemented and verified end-to-end against the dev DB. All three fixes typechecked (both repos) and confirmed via direct API/DB round-trips (Playwright unavailable — a real browser session from another concurrent session held the lock).
- **Dashboard redesign requested 2026-08-27, not yet designed.** Marcelo wants two new elements on the Dashboard: (1) a **Portfolio view** — full breakdown of what's held: cash, stocks, options, presumably replacing/extending today's Allocation + Cash cards; (2) a **Latest Events feed**, grouped by strategy, covering at minimum: new position, closed position, expired position, assigned position. Each event needs a details view explaining what happened in plain terms specific to that event type — e.g. a new covered-call position should show "option sold for $X, stock bought for $Y, cash −$Z" (the actual cash delta sign/direction still needs confirming against real fill data). Different event types will need different context fields (a close needs realized P&L, an assignment needs the resulting share delta and strike, an expiry needs which leg expired worthless, etc.) — no field list has been drafted yet. **Before implementing**: (a) fully analyze each event type to decide exactly what data to surface and how to phrase it per strategy (CC vs CSP will differ), likely needs sign-off per the formulas working agreement since this touches P&L/cash math; (b) audit whether the event set above is complete — check for missing event types the platform should also be surfacing (e.g. rolls, partial closes/partial fills, cancelled orders, reconciliation-detected drift/orphans as an "unstructured" event, dividend/corporate-action events if any). No schema decision made yet on whether this is a new `events`-style table, a derived view over `trades`/`position_legs`/`order_requests`, or something else.

## Order execution platform (started 2026-08-24)

**Decision**: IBKR is the source of truth for positions, not the app's own database — the app places real orders (paper Gateway) and continuously syncs IBKR's reported state back into Postgres. **Why**: a real prod bug (AAOI's naked covered call from a free-typed quantity field with no cross-leg validation) showed manual position entry could silently drift from reality; the fix was to remove manual entry entirely in favor of real order placement.

**What's built and verified against real paper trades** (opens, fills, a full close, a cancel of a live order, and reconciliation-driven self-healing have all been confirmed against real IBKR activity — a real roll has not yet been exercised):
- `order_requests` table — the web-to-worker order queue (`pending_confirmation → confirmed → submitted → filled|partially_filled|cancelled|rejected|error`).
- `src/ibkr/ibkrGatewayPersistentConnection.ts` — the app's one long-lived IBKR connection (fixed `clientId=42`, everywhere else uses per-request random ids), reconnects with backoff.
- `src/ibkrGatewayWorker.ts` (separate `worker:` process, deployed to the VPS not Heroku — see [[project_worker_runs_on_vps]]) — places orders (single-leg, or an atomic BAG combo for multi-leg), tracks fills, and runs `reconcilePositionsFromIbkr()` every 60s + on connect, with an in-flight mutex so passes never overlap. Pairing heuristic: long stock + short call same symbol → `covered_call`; any short put → `cash_secured_put`, handled unconditionally and independently of a covered call on the same symbol (fixed 2026-08-27 — a real AAOI position had its put stranded as `unstructured` forever after its sibling call/stock rolled off into a separate covered_call position, since the put was only ever considered when no stock/calls existed at all); anything else → `unstructured` ("Needs Review" badge, never hidden). Each distinct short-call conId now splits into its own position + proportional stock slice (fixed 2026-08-25 after MU showed 2 calls wrongly merged into one position).
- **2026-08-27: `ibkrGateway*` naming convention adopted.** Every file whose code ships as part of the VPS-deployed worker (`ibkrGatewayWorker.ts` and its `src/ibkr/ibkrGateway*.ts` imports) is prefixed `ibkrGateway`, so it's obvious at a glance which edits need a manual `npm run deploy:worker`, not just a `git push`.
- **2026-08-27: worker silently disconnected for 8+ hours, root-caused and fixed.** `ibkrGatewayWorker.ts`'s persistent IBKR connection dropped at 12:50 UTC; its reconnect loop logged one failed attempt then went silent forever, even though `systemctl status` still showed the process "active (running)". Root cause: `ibkrGatewayTunnel.ts`'s `openIbkrTunnel()` awaited `sshClient.connect()` with no timeout — a stalled SSH attempt left that promise permanently pending, freezing the reconnect loop with no further logs (distinct from the already-fixed unhandled-pipe-error crash noted below). While disconnected, IBKR-side closes on 4 MU/AAOI legs never synced back to `position_legs`, surfacing as false "position reconciliation" alerts from the separate hourly health-check connection (which uses its own short-lived connection via `connectIbkr.ts` and kept working throughout). **Fixed**: `openIbkrTunnel()` now has its own 15s timeout on the SSH connect phase. Worker restarted and redeployed same day; stale legs self-healed via the normal reconciliation pass within seconds of reconnecting, confirmed via direct DB check and the next hourly health-check run logging zero discrepancies.
- `src/routes/positions.ts` — build-then-confirm pattern everywhere: `POST /orders` (preview, writes nothing) → `POST /orders/:id/confirm` (transmit gate, `pg_notify`s the worker) → `POST /orders/:id/cancel` (works pre-submission instantly; once submitted, flips to `cancel_requested` and the worker calls `ib.cancelOrder()` — confirmed against a real live order). Roll and close follow the same pattern. Close accepts `contractsToClose`, option-leg-driven (stock quantity always derived, never independently settable).
- `src/ibkr/checkPositionReconciliation.ts` — compares every open local leg against IBKR's real holdings, flags orphans/mismatches in either direction. Wired into the hourly `ibkr_health_check` job. Detection only, no auto-fix. Verified against real forced drift.
- Frontend: shared `OrderReviewPanel.tsx` (review → explicit Confirm → poll to terminal status) used by New Position, Close, and Roll. Genosuke's three order tools call build+confirm sequentially, gated by Telegram's existing Yes/Cancel.

**Root cause chain for why every order was initially rejected (all fixed, 2026-08-24)**: `READ_ONLY_API=yes` was a leftover default (fixed → `no`); IBC's automated config dialog sequence was silently interrupted by an unhandled disclaimer dialog, so the Read-Only fix never actually saved until manually clicked through once via `xdotool`; IBKR error codes 399 and 2100-2169 are informational, not rejections, and were being mishandled as such. **Residual risk closed 2026-08-24**: a bind volume for the Gateway's settings dir now survives full container recreation, not just restarts — verified against a real `docker compose down/up`.

**Recurring bug class hit repeatedly across this build, now fixed everywhere found**: IBKR reports `""`/`0` (not `null`/`undefined`) for a contract field that doesn't apply to that security type (a stock's expiry/strike, an option's multiplier under some paths) — `??` doesn't catch it, `||` does. Hit and fixed for: stock leg `expiry_date`, `strike_price`, `multiplier` (this last one silently zeroed the stock leg's entire contribution to unrealized P&L until fixed); option leg `entryPrice`'s `multiplier` divisor; `sourceAlertId` from Genosuke's manual-entry path. See [[project_ibkr_empty_string_not_null]].

**Other real bugs found via real fills/closes, all fixed**: opening fills weren't recorded into `trades` at all for a brand-new position (fixed by buffering unmatched executions and draining them once the leg exists); `execution.time`'s IBKR format (`"YYYYMMDD HH:mm:ss <IANA zone>"`) isn't parseable by `new Date()` (fixed with a dedicated parser, DST-safe); a partial closing fill was marking the whole leg closed regardless of remaining IBKR-held quantity (fixed — only the reconciliation pass's zero-holding check now closes a leg); BAG combo leg ratios needed reducing to their GCD, not sent as raw quantities (IBKR rejected a 3-contract close outright until fixed); multi-batch opening fills for the same conId weren't accumulating into `position_legs.quantity` (fixed — quantity/entry_price now resync from IBKR's current truth on every pass, safe since IBKR always reports the current total, never an increment).

**Operational**: worker deploys are manual to the VPS (`npm run deploy:worker`), not automatic from a GitHub push — see [[project_worker_runs_on_vps]]. A dedicated hourly SSH health-check (forced-command key) verifies the worker's systemd unit is active and restarts it if not, verified against a real forced outage 2026-08-25.

**Two more real bugs found and fixed 2026-08-27**: (1) an option leg that expired (or was assigned) with no matching `trades` row — the normal case for expiration, since IBKR never generates an execution for it — got `exit_price = null` instead of `0`, silently dropping it out of `realizedPnl`'s `SUM(... WHERE exit_price IS NOT NULL)` and reporting $0 P&L for every expired short option instead of the full premium kept; fixed in `reconcilePositionsFromIbkr`, backfilled two already-affected MU legs in prod, and added a Telegram + in-app toast notification (`notifyPositionExpired`, shared `formatPositionExpiredMessage`) for future expiry/assignment closes. (2) confirming an order tied to a trade alert flipped `trade_alerts.status` to `approved` with nothing ever reverting it if the order was then cancelled/rejected without filling — the alert stayed permanently unusable, breaking "cancel and resubmit with a different quantity." Fixed: both the local-cancel path and the worker's terminal-order-status path now revert the alert back to `pending` (`revertSourceAlertToPending.ts`).

## Open decisions (blocking further design)
- [ ] Cloudflare custom firewall rules beyond Bot Fight Mode — no specific rule identified yet, optional hardening.
- [ ] `reqFundamentalData` (financials/ownership/analyst estimates) — blocked, separate paid IBKR entitlement (error 10358). Not needed by anything shipped.
- [ ] IBKR historical backfill coverage (OHLCV, especially `OPTION_IMPLIED_VOLATILITY`) needs empirical per-symbol testing — not guaranteed for every ticker.
- [ ] Assignment preference/risk tolerance varies **per ticker**, not just per strategy (e.g. OK holding SMH long-term, not SNDK). Undecided whether this needs a structured field on `shortlist_entries` or stays in free-text `notes`.
- [ ] Market data subscription (~$10/mo) still blocked on Juan's account funding threshold. Blocks real-time pricing/screening only, not paper Gateway connectivity.
- [ ] `avg_option_volume` (generic tick 105) never arrives under delayed-data entitlements (confirmed IBKR limitation, not a bug) — revisit once real-time is active; may need deriving from option-chain volume instead, or dropping the field.
- [ ] Shared `DataTable` doesn't follow menaris-admin-app's table-sm/reduced-font/abbreviated-header convention (flagged 2026-08-13). App-wide pattern change, deliberately not done without checking first.

## Open decisions — Juan's Iorio Upgrades feedback (2026-08-24)
Full IBKR trading-book/depth visibility, full option chain w/ all bid-ask, richer trade tab, and a new upcoming-events tab. Broken into independently-decidable pieces:
- [ ] **[HIGHEST PRIORITY] Full IBKR API audit once real-time market data is enabled** — Marcelo wants the whole IBKR wrapper rethought (today's error handling called "flaky and obscure"). Once real-time is live: measure real latency per call, verify completeness of data actually received, look for faster/more robust alternatives per endpoint. On delayed data, a ~100-160-contract option chain averaged only ~83% price+delta completeness within an 8s streaming window (snapshot mode tested clearly worse, ~54%) — real-time may behave completely differently and every tradeoff needs re-measuring, not assumed to carry over.
- [ ] Order confirmation needs real-time prices and the live option chain (Juan) — likely gated on the same real-time subscription blocker.
- [ ] Explore alternative IBKR order types for better fill price (marketable limit, midpoint/peg, adaptive algos) — not scoped yet.
- [ ] Click-to-open per-symbol depth-of-book — investigated 2026-08-25, actually blocked: `SMART` routing doesn't support depth at all (error 10092), and a real exchange requires its own paid depth subscription (error 10089, ~$1.50–$25/mo/exchange). Revisit once Juan confirms a depth subscription.
- [ ] Continuous real-time streaming on Screener/Positions (vs. today's daily-cached-snapshot design) — real architecture change, gated on the real-time subscription blocker.
- [ ] Market data line limit / cost to raise — default min 100 lines; Quote Booster packs $30/mo per +100 L1 lines, max 10 packs (~$300/mo cap). Juan should confirm exact current pricing in his own account.
- [ ] **Company earnings/investor-day dates — needed for both display and trade-alert filtering (2026-08-28)**: real lead is IBKR's Wall Street Horizon ("Enchilada") feed, separate from the blocked `reqFundamentalData`; price gated behind Juan's account, needs him to check. Marcelo's actual requirement is stronger than just showing the date — trade-alert generation should not suggest opening a position whose expiry would leave it open across an earnings release. No design done yet on where in `generateTradeAlertCandidates.ts`'s pipeline that filter would sit, or whether it's a hard exclusion vs. a flagged/deprioritized candidate.
- [ ] Global macro economic calendar (CPI, FOMC, etc.) — no IBKR API for this. Needs a separate vendor decision, not started.
- [ ] **Ex-dividend dates — needed to gate new CC trade-alert candidates (2026-08-28)**: capture each ticker's next ex-div date and use it to cap eligible expiries when generating covered-call candidates — e.g. ex-div in 20 days means only expiries before that are eligible, so an early-exercise-for-dividend event can't land mid-position. **CC-only** — no early-exercise-for-dividend mechanism exists on short puts, so this must not touch CSP candidate generation. Source not yet identified (same IBKR account/feed question as earnings dates above may apply — check whether Wall Street Horizon or another entitlement covers this too). No design done yet on where in `generateTradeAlertCandidates.ts` this filter would sit.
- [ ] ETF constituents (e.g. SMH holdings) — no IBKR API for this either. Needs a separate reference-data vendor, not started.

## Strategy domain notes (from Juan Dominguez, developer friend with trading domain knowledge, via WhatsApp 2026-08-12)
Captured for the trade-alert-generation algorithm design — don't lose it.
- **Trade timing**: avoid executing right at market open; wait ~30 min. Anecdotal: Nasdaq direction after the first 30-min candle often predicts the day's trend (unverified). A sharp opening drop may mean CSP premiums pay more that day, and vice versa for calls.
- **Covered calls**: sell against anything held, always, ideally bought on a dip first. Generally *want* assignment (premium + appreciation to strike = max profit) — varies per ticker based on comfort holding long-term if not assigned. Higher/further strike = less premium now but keeps the stock for continued call-selling.
- **Cash-secured puts**: assignment usually *not* wanted. Worth checking moving averages/trend as a screening filter. Worst-case defense if assigned and stock drops: sell covered calls against the assigned shares to work back to breakeven.
- **Rolling**: when a short option has decayed to near-worthless, often worth buying it back even at a small cost to free up collateral, then selling further out for fresh premium.
- **Scenario comparison**: when choosing between candidate strikes, want P&L outcomes compared side by side under a price scenario — satisfied today by Trade Alerts' payoff-summary cards; a full interactive chart per candidate is deferred.
- **Overall framing**: covered calls are comparatively lower-risk; CSPs need more scrutiny since assignment means acquiring stock at strike. System objective: maximize premium income plus the "wheel" cycle (assigned → redeploy cash → repeat). No schema implication — operational philosophy executed via Screener/Trade Alerts.

## Decisions made
- Stack: Node.js backend, React frontend, PostgreSQL via Heroku Postgres Essential-1, Heroku hosting.
- UI template: Tabler (`@tabler/core` + `@tabler/icons-react`). Charting: ApexCharts (aggregate) + TradingView's lightweight-charts (OHLC/candlestick, our own component ported from menaris-admin-app's `LwChart.js`). Payoff diagrams are custom computed line/area series.
- Two users, same access level, no per-role permissions.
- Personalization (table column visibility, UI prefs) stored client-side in localStorage only.
- Strategies implemented in code, not via UI; schema/platform supports multiple strategies running in parallel. v1 scope: covered calls + cash-secured puts, both from day one.
- Historical options data / deep backtesting: shelved — 10 years of full-detail data doesn't fit a 10GB Postgres budget, most vendors' ToS block bulk export. Lightweight forward-simulation (self-accumulated data) planned instead.
- All market data (screening + pricing) sourced directly from IBKR, no third-party vendor. Realized/unrealized P&L sourced live from IBKR directly, not computed from our own records.
- **IBKR connectivity: dedicated Hetzner VPS** — Heroku dynos restart ~daily with an ephemeral filesystem (wrong fit for a stable Gateway needing to avoid repeated 2FA), the user's Mac can't guarantee market-hours uptime. `gnzsnz/ib-gateway-docker` + `@stoqey/ib`. Heroku-to-VPS bridge is an SSH tunnel with a dedicated, restricted key (`permitopen`+`command=` clause, `restrict` alone does not block exec-channel command execution). Two separate Gateway containers (`ib-gateway-paper`/`ib-gateway-live`) for independent restart schedules and crash isolation.
  - VPS live since 2026-08-13 at `142.132.185.128`. IBKR does not support a second/secondary user on a paper account — multi-user needs the live account. Juan owns his own IBKR credentials end-to-end.
- Domain: ioriore.com, `app.`/`api.` subdomain split via Cloudflare + Heroku ACM.
- Screener: daily cached snapshot (`market_data_snapshots`), not live-per-view IBKR queries. Made strategy-agnostic 2026-08-25 — `strategy_key` dropped from `shortlist_entries` (a shortlisted ticker is now scanned by every strategy's trade-alert job), after discovering CSP alerts never fired because no ticker had ever been shortlisted under that strategy.
- Shared `Spinner` component standardized for every non-immediate operation. See [[feedback_ui_loading_and_modal_dismiss_conventions]].
- Database schema (conceptual, finalized) — see below. Legs-based positions so future multi-leg strategies don't need a rewrite.
- Ticker Detail modal scoped around live pricing/chart/option chain (not fundamentals), platform-wide shared modal for every ticker symbol in the app.
- Option chain scope: strategy-relevant window (near-the-money strikes across nearby expiries in the DTE band), not the full chain — stays under IBKR's ~100 concurrent-market-data-line limit. Widened 2026-08-20 to include weeklies.
- Authentication: password-based, Argon2id, `express-session`+`connect-pg-simple` (Postgres, not Redis). No self-service password reset — CLI script only.
- **IV Rank formula** (approved 2026-08-21): `(current IV − min IV) / (max IV − min IV) × 100` over up to a trailing 252-trading-day window. Shown with the actual window size (e.g. "IV Rank (45d)") rather than hidden until a full year accumulates. Requires ≥2 distinct-IV points and a non-zero range, else null.
- **Yield/ranking formula** (approved): `(premium/capitalAtRisk)×(365/dte)`, annualized. See "Currently open" above for the follow-up discussion on what it doesn't account for.
- **Roll trigger thresholds** (approved 2026-08-21): close/roll when the leg has decayed to ≤50% of credit collected (tastytrade's "50% rule") OR DTE drops to ≤21, whichever first. No delta-based "avoid assignment" trigger — no rigorous backing found and it cuts against covered calls generally *wanting* assignment.
- **Allocation/exposure formula** (approved 2026-08-25): every % is against total account net liquidation value (positions + cash), not sum-of-positions, with an explicit "Unallocated" cash row.

## Database schema (conceptual — matches what's live)
- **users** — `id, email, display_name, password_hash, created_at`. Argon2id hash. No reset flow — CLI script only.
- **tickers** — `id, symbol, company_name, sector, ibkr_contract_id, created_at`.
- **shortlist_entries** — `id, ticker_id, added_by_user_id, added_at, removed_at, notes`. `removed_at` soft-deletes. (Name predates the strategy-agnostic decision above.)
- **positions** — `id, strategy_key, ticker_id, status (open/closed), ibkr_account_id, opened_at, closed_at, notes`. Strategy-agnostic shell.
- **position_legs** — `id, position_id, leg_type (stock/option), side (long/short), quantity, option_type, strike_price, expiry_date, multiplier, ibkr_contract_id, entry_price, entry_at, exit_price, exit_at`. `multiplier` isn't always 100 — see [[project_position_leg_multiplier_vs_quantity]].
- **trades** — `id, position_leg_id, ibkr_order_id, ibkr_exec_id (unique), side, quantity, price, commission, realized_pnl, executed_at, raw_ibkr_payload (jsonb)`. Execution blotter.
- **order_requests** — the web-to-worker order queue (see Order execution platform above). `payload` jsonb: `{symbol, strategyKey, legs: [{role, action, symbol, quantity, unitPrice, strike?, expiry?, right?, ibkrContractId?, positionLegId?}]}`.
- **trade_alerts** — `id, strategy_key, ticker_id, suggested_structure (jsonb), rationale, status (pending/approved/rejected/modified/expired), reviewed_by_user_id, reviewed_at, resulting_position_id`.
- **account_pnl_snapshots** — `id, snapshot_date, daily_pnl, realized_pnl, unrealized_pnl, net_liquidation_value, net_cash_flow, captured_at`. `daily_pnl` = net-liq delta minus `net_cash_flow`.
- **position_pnl_snapshots** — `id, position_id, snapshot_date, realized_pnl, unrealized_pnl, market_value, captured_at`. One row/day per open position.
- **daily_price_bars** — `id, ticker_id, trading_date, open_price, high_price, low_price, close_price, volume, captured_at`. Backs 1Y/5Y/All chart ranges (5Y/All resampled weekly in SQL) and Price Performance.
- **intraday_price_bars** — `id, ticker_id, bar_size, bar_time, ...OHLCV`, `(ticker_id, bar_size, bar_time)` unique. Backs 1D/5D/1M/3M/6M chart ranges. No split-adjustment (unadjusted `TRADES` bars) — a real stock split needs a manual `DELETE` on that ticker's cached rows.
- **option_chain_params** — one row per ticker: expirations/strikes arrays + `fetched_at`. 24h refresh.
- **option_chain_strike_checks** — `(ticker_id, expiry, strike)` unique, `exists` boolean + `checked_at`. `exists=true` cached forever; `exists=false` gets a 24h re-check window (an exchange can add a new strike after a large move).
- **market_data_snapshots** — `id, ticker_id, snapshot_date, implied_volatility, avg_option_volume, captured_at`. `avg_option_volume` always null under delayed data (see Open decisions).
- **job_runs** — `id, job_name, started_at, finished_at, status (running/success/failure), error_message, details (jsonb)`. Backs System Health + Telegram alerts + Watchdog.
- **strategy_settings** — `id, strategy_key, delta_target_min/max, dte_target_min/max, max_position_pct_of_portfolio, max_aggregate_collateral_pct, max_concentration_per_ticker_pct, max_concentration_per_sector_pct, min_cash_reserve_pct, updated_at`.
- **genosuke_chat_messages**, **genosuke_preferences** — Genosuke's rolling 24h chat history and durable cross-conversation preferences (see Built section).

## Built
Everything below is real, working code, verified end-to-end (migrations against real databases, live HTTP/Playwright testing, real IBKR round-trips where applicable). Full debugging narratives condensed here — see git history/commit messages for the date mentioned if the full story is ever needed again.

**Platform / infra**
- Both apps auto-deploy from GitHub → Heroku on push to `main`; Postgres Essential-1, migrations run via a `release:` Procfile phase.
- Auth (Argon2id + `express-session`/`connect-pg-simple`), `scripts/manage-user.ts`, global crash handlers (`installCrashHandlers.ts` — log + Telegram alert + `process.exit(1)`, verified against real forced crashes), global Express error handler.
- IBKR client (`src/ibkr/`) — SSH-tunneled `@stoqey/ib` connection, explicit-required env vars, verified via real round-trips. Recurring gotchas fixed across the build: missing `clientId`/connect timeout could hang a second concurrent connection forever ([[project_ibkr_connection_clientid_timeout]]); `once()` listeners could orphan other concurrent id-scoped listeners ([[project_eventemitter_once_concurrent_listeners]]); ambiguous `reqContractDetails` calls get throttled, fully-qualified ones don't ([[project_ibkr_option_strikes_per_expiry]]); an unhandled `'error'` on the SSH-forwarded pipe stream crashed the whole process on any transient network blip (fixed 2026-08-27, `ibkrGatewayTunnel.ts`); a stalled `sshClient.connect()` inside the same file had no timeout and could freeze the worker's reconnect loop forever with no further logs (separate bug, also fixed 2026-08-27 — see the "Order execution platform" section for the 8-hour outage this caused).
- **Caching layer, built 2026-08-27 after a full IBKR call-site inventory** to cut throttling risk: contract details (conId/company/sector) cached on the `tickers` row, effectively-immutable; option chain params + per-strike existence checks cached (24h refresh, see schema above) — both proven via sentinel-poisoning tests (overwrote a cached value, confirmed the app actually reads the poisoned value rather than a fresh live one, then restored it). Chart bars (daily + new intraday table) cache-then-delta-fetch instead of re-pulling full history on every range switch. Genuinely live-vs-live-only data (positions, account cash/exposure, streaming quotes) deliberately left uncached.
- `sync-dev-db` script pulls prod DB to local dev, preserves local users across the sync. See [[project_sync_dev_db_script]].

**Frontend shell** — React 19 + Vite + Tabler + ApexCharts + lightweight-charts. Session-aware routing, all 8 screens, column-visibility gear-icon `DataTable` (localStorage-persisted), dark/light theme toggle, mobile-verified at 390×844 throughout. Shared `formatters.ts`/`payoff.ts` libraries.

**Screener** — single cross-strategy monitored-ticker list (`LEFT JOIN LATERAL` on latest snapshot), IV Rank column (formula above), search-as-you-type (`reqMatchingSymbols`, 400ms debounce). Ad-hoc "+ New Position"/"Trade" entry point removed 2026-08-27 per Marcelo's call — new positions now only originate from a Trade Alert or Genosuke.

**Ticker Detail modal** — platform-wide shared modal (SSE-streamed), live pricing/chart/strategy-relevant option chain with Greeks. Redesigned 2026-08-26 to fold Trade Alerts into the modal itself (pending alerts table above the chain, matched strikes get a clickable order-setup panel), prices switched to genuinely continuous live streaming (not polled/throttled). Real IBKR errors now surface for this screen's calls instead of generic timeouts. Several rounds of live-testing polish on 2026-08-27 (spot-price marker row, `formatCurrencyTrimmed` for whole-dollar strikes, stale-price-freeze bug on IBKR's `-1` sentinel tick fixed across all pricing paths, Order Setup preview panel gained Max Gain/Max Loss/Breakeven).

**Order Review panel** (`OrderReviewPanel.tsx`, shared across New Position/Roll/Close) — DTE, capital-at-risk/EXP%, max gain/loss/breakeven, live bid/ask/spread/IV/Greeks via a genuinely live SSE quote stream. Opening orders get a live delta-compliance check (`checkDeltaCompliance` against `strategy_settings`) that disables Confirm until a compliant quote arrives — fails closed. Also shows live cash-sufficiency (Available Cash / Cash Required / Cash After Trade), netting cash already committed to open CSPs.

**Positions** — legs-based CRUD, live per-leg Greeks, live unrealized P&L (`GET /positions/pnl`), EXP$/EXP% exposure columns. Close accepts partial-quantity downsizing (option-leg-driven, stock always derived). No manual position-creation entry point (see Screener above).

**Trade Blotter** — real fills only (`trades`), merged with in-flight `order_requests` rows in one timeline with a live "IBKR State" column; relative-time labels; Cancel button on any `pending_confirmation` order.

**Trade Alerts** — full pipeline: scheduled scan/ranking (see Scheduled jobs) + review UI. Redesigned 2026-08-27 to one card per ticker (New Trade / Roll tables) instead of one per alert; per-alert Dismiss/Reject removed in favor of scan-supersedes-stale; per-ticker on-demand Refresh added; Review opens the Ticker Detail modal with the order panel already open. Roll alerts execute a real leg swap (`POST /positions/:id/roll`), not just a suggestion.

**Risk & Limits** — `strategy_settings` thresholds with field help tooltips; live exposure/concentration by ticker/sector against those thresholds (with an "over limit" badge); allocation breakdown by strategy/top-position/sector (see Decisions made formula).

**Dashboard** — aggregate P&L (day/week/month/year, calendar-based), realized/unrealized state, per-strategy breakdown (realized P&L sourced from `position_legs` directly so a closed position's gain isn't dropped), Allocation card, Cash card.

**Price Performance** (8th screen, 2026-08-25) — 24/48/72hr/1-week/1-month price changes and daily/weekly/monthly lows/highs per shortlisted ticker, computed entirely from `daily_price_bars` (no live IBKR calls). 24/48/72hr are trading-day close deltas, not true rolling 24-hour windows; week/month figures use a rolling calendar-day window, not week-to-date/month-to-date.

**System Health** — reads `job_runs`, on-demand real IBKR handshake check, collapsible issue summary (not a raw JSON dump).

**Genosuke — conversational Telegram bot** (built 2026-08-21/22, ported/adapted from menaris-admin-api's "Jack"). Webhook-based (switched from long-polling 2026-08-24, `POST /genosuke/webhook`), chat-ID allowlist gate, tool-calling loop against the app's own REST endpoints as an authenticated service user (not reimplemented route logic — see [[project_internal_api_client_pattern]]). Three tool tiers: read-only (10, no confirm), low-stakes write (5, immediate), financial-write (5, gated behind a real Telegram inline Yes/Cancel keyboard, in-memory `PendingConfirmation` with a 10-min TTL). Live in prod since 2026-08-24. Per-chat history moved off an in-memory `Map` (lost on every deploy) to a DB-backed `genosuke_chat_messages` table with a genuinely rolling 24h window (2026-08-27); `genosuke_preferences` table + `save_preference`/`forget_preference` tools let Marce/Juan set durable cross-conversation instructions — Genosuke only offers to remember on its own initiative for durable behavioral instructions, never one-off requests.

## Screen list (finalized, 8 screens + shared modals, all built)
General (cross-strategy): Dashboard, Positions, Trade Blotter, System Health, Risk & Limits, Screener, Price Performance.
Strategy-specific (tabs within the screen): Trade Alerts.
Shared modals: Ticker Detail (live pricing/chart/option chain w/ Greeks, platform-wide — not yet showing position/strike/entry markers on the chart), Position Detail (chart, payoff diagram, price targets, close triggers).

## Scheduled jobs (Heroku Scheduler) — 5 daily/hourly job names, 6 entries, all skip weekends
All 6 entries set to Basic dyno size in the Scheduler dashboard (manual, no CLI/API access to that setting).
1. **Daily price/IV capture** (`job:daily-market-data`, 9:00 PM UTC) — **currently broken, see "Currently open" above.**
2. **Daily P&L snapshot** (`job:daily-pnl-snapshot`, 9:30 PM UTC) — `account_pnl_snapshots`+`position_pnl_snapshots` from IBKR.
3. **Trade Alert generation** (`job:trade-alerts`) — runs twice daily: 10:00 PM UTC (after #1/#2's EOD data) and 1:30 PM UTC (market open, added 2026-08-25 so a full freshly-scanned list exists right at open, not just overnight EOD suggestions). Redesigned 2026-08-27 — see "Currently open" above.
4. **IBKR Gateway health check** (`ibkr_health_check`, hourly at :00) — real handshake-based check (fixed 2026-08-21 after a `nc -z`-only version stayed green during a real outage), plus the worker-systemd check and the position-reconciliation drift check (see Order execution platform).
5. **Watchdog** (`job:watchdog`, daily 10:30 PM UTC) — checks jobs 1-3 ran today, health-check ran within 2h, nothing stuck `running` 30+ min. Telegram-only on actual findings.

### Telegram notification rules
- Fail-safe ordering: `job_runs` written first, Telegram attempted after, never masks a job failure.
- Alerts on every failure today, not state-transition-only. Trade Alerts notifies on every new batch. No daily success heartbeat otherwise.

## Staging environment (proposed 2026-08-27, not started — needs a full design pass before building)
**Goal**: flip the current single environment so the existing `app`/`api` subdomains point at the **live** IBKR account and become real prod, and a new **staging** environment (`app-stg`/`api-stg`) is created hitting the **paper** account, replicating the current setup for testing/development.

Requirements as specified, none built yet:
- [ ] Staging replicates the current stack (web, API, Postgres, VPS worker) scoped to `-stg` and the paper account.
- [ ] Existing environment repointed from paper to live — becomes real prod.
- [ ] Staging has no scheduled/daily jobs — manual/on-demand only.
- [ ] Staging gets its own Genosuke instance posting to a separate Telegram channel.
- [ ] Under consideration: a staging-only screen to sync prod DB → staging (similar to [[project_sync_dev_db_script]]).
- [ ] Auto-deploy applies to staging only — prod deploys become manual/deliberate.
- [ ] Deployment strategy must be "failproof" and cover all three moving parts together (Heroku code, DB migrations, the VPS worker — see [[project_worker_runs_on_vps]]).
- [ ] Considering laptop-driven deploy scripts Claude executes/monitors, if more reliable than Heroku's own flow.
- [ ] Needs a post-deploy smoke-test suite covering web/API/DB/IBKR worker+Gateway connectivity.
- [ ] Needs a clean, well-defined rollback path.

**Explicitly deferred — do not start building any of this yet.** Needs a design pass first to ensure prod/staging are fully isolated (DB, IBKR connections/clientIds, Telegram bots, secrets — no shared state that could let staging affect prod) and that staging is actually representative enough of prod's real IBKR-connection quirks ([[project_ibkr_connection_clientid_timeout]], [[project_ibkr_sibling_wrapper_handler_drift]]) to catch real issues before they reach prod.

## Optional / future ideas (not being built unless requested)
- Full historical backtesting engine using purchased historical options data (10 years) — cost/ToS permitting. Lightweight forward-simulation (self-accumulated data only) is planned instead, not optional.
- IBKR chart embedding for open positions — superseded by the decision to build our own via lightweight-charts.
